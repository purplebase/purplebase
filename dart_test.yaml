# Test configuration for models package
# Run tests serially to prevent singleton sharing issues
concurrency: 1

# Test timeout
timeout: 7s

# Platform configuration
platforms:
  - vm

# Reporter configuration
reporter: compact 